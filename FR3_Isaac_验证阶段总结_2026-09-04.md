# FR3 Isaac Sim 迁移与受限下降验证总结（2026-09-04）

## 1. 目的与工程位置

目标是在 Isaac Sim 中完成 FR3 + Robotiq 2F-85 对香蕉的视觉定位和抓取。当前尚处于**无闭爪、无抬升的运动链路验证阶段**，不能宣称抓取成功。

实际工程路径：

```text
C:\Users\20855\Desktop\work\Issac_fr3\FR3
```

目录名是 `Issac_fr3`。WSL 路径为：

```text
/mnt/c/Users/20855/Desktop/work/Issac_fr3/FR3
```

ROS Domain ID 为 `42`。Isaac Sim 运行在 Windows；ROS 2 Humble、MoveIt 和任务节点运行在 WSL Ubuntu 22.04。

## 2. 已阅读和核对的文件

### 交接和总体架构

- `C:\Users\20855\Desktop\FR3_Isaac抓取任务_阶段交接文档_2026-09-04.md`
  - 记录了五段链路：Isaac 场景桥接、YOLO RGB-D、轨迹代理、夹爪代理、MoveIt + 抓取节点。
  - 指出旧架构的主要风险是 MoveIt 多点轨迹被压缩为单末点，并要求先验证受限下降。

### ROS 2 / MoveIt

- `src/fairino3_v6_moveit2_config/src/pick_banana_node.cpp`
  - 负责目标订阅、预抓取、下降、夹爪和抬升。
  - 原默认流程会继续执行下降后的补偿、闭爪和抬升；不适合当前安全验证阶段。
- `src/fairino3_v6_moveit2_config/launch/pick_banana.launch.py`
  - 暴露抓取节点参数和 MoveIt 启动配置。
- `src/fairino3_v6_moveit2_config/CMakeLists.txt`
  - `pick_banana_node` 已作为 ROS 2 可执行文件安装。

### Windows Isaac 桥接

- `isaac_fr3_sim/scripts/run_fr3_scene.py`
  - 启动 Isaac、加载 USD、发布 `/joint_states`、接收 `/fairino3_controller/joint_trajectory`。
  - `TrajectoryExecutor` 已能验证并插值多点 `JointTrajectory`。
  - 有实际 USD 关节限位和 `±2π` 运行时安全停止保护。
- `isaac_fr3_sim/scripts/trajectory_action_proxy.py`
  - 将 MoveIt 的 `FollowJointTrajectory` Action 转发给 Isaac。
- `isaac_fr3_sim/scripts/gripper_action_proxy.py`
  - 只有收到 `GripperCommand` goal 时才发布夹爪位置命令；空闲运行不会发送夹爪命令。
- `isaac_fr3_sim/run_isaac_fr3.ps1`、`run_moveit.ps1`、`run_target_snapshot.ps1`、`run_trajectory_action_proxy.ps1`
  - Windows/WSL 的启动入口。

### 场景和辅助检查

- `isaac_fr3_sim/scripts/inspect_collisions.py`
- `isaac_fr3_sim/scripts/validate_scene_collisions.py`
- `isaac_fr3_sim/scripts/test_scene_physics.py`
- `isaac_fr3_sim/README.md`

README 中“完整抓取至抬升”的说明已落后于当前安全状态；当前不得按该说明直接运行完整抓取。

## 3. 已完成的修复和改动

### 3.1 恢复 ROS 2 工作空间

用户此前删除了 `build`、`install`、`log`，因此 `install/setup.bash` 丢失。已重新执行：

```bash
source /opt/ros/humble/setup.bash
cd /mnt/c/Users/20855/Desktop/work/Issac_fr3/FR3
colcon build --symlink-install
```

后续每个 WSL ROS 终端都应 source：

```bash
source /opt/ros/humble/setup.bash
source /mnt/c/Users/20855/Desktop/work/Issac_fr3/FR3/install/setup.bash
export ROS_DOMAIN_ID=42 ROS_LOCALHOST_ONLY=0 RMW_IMPLEMENTATION=rmw_fastrtps_cpp
```

遗漏最后一条导出不会改变已记录的成功结果：成功日志已经证明各节点实际在同一 Domain 内通信。但后续命令仍应明确设置它，避免新终端继承不同环境。

### 3.2 修复 Windows 桥接自检中的失效断言

`run_fr3_scene.py --self-test-trajectories` 初始有两处与当前安全参数不一致的测试数据：

- 自检给 `j6=7 rad`，超过全局 `±2π` 限制；
- 夹爪限速为 `0.1 rad/s`，`0.1 s` 内应移动 `0.01 rad`，旧断言错误期待 `0.05 rad`。

修正后自检通过：

```text
[bridge] trajectory self-test passed
```

### 3.3 删除轨迹代理的末点压缩

已修改：

```text
isaac_fr3_sim/scripts/trajectory_action_proxy.py
```

旧行为：

```text
MoveIt N 个点 -> 仅发送最后 1 个点
```

新行为：

- 丢弃时间为 `0` 的起始点（Isaac 桥要求首点至少 `0.1 s`）；
- 保留其余全部 waypoint；
- 保留相对时间轮廓，并在必要时按比例放慢到不低于 `max_delta / 0.2 rad/s`；
- Action 仍只按最终关节误差判定成功。

此 Python 修改不需要 `colcon build`。

### 3.4 新增单次受限下降模式

已修改并重新构建：

```text
src/fairino3_v6_moveit2_config/src/pick_banana_node.cpp
src/fairino3_v6_moveit2_config/launch/pick_banana.launch.py
```

新增参数：

```text
single_descend_enable := false
single_descend_max_distance := 0.01
single_descend_min_height_above_target := 0.08
```

启用后行为：

1. 执行完整多点预抓取；
2. 跳过夹爪打开/闭合、姿态二次修正和 XY refine；
3. 从实际 TCP 读取高度；
4. 使用校正后的抓取参考高度 `tz_obj + min_height` 作为下限；
5. 仅计算一次向下的 Cartesian 路径，要求完整度至少 `99.9%`；
6. 不允许 joint-space fallback、补偿、再次下降、夹爪或抬升；
7. 输出几何关系后退出。

构建验证：

```text
Finished <<< fairino3_v6_moveit2_config
Summary: 1 package finished
```

### 3.5 新增场景碰撞隔离开关

已修改：

```text
isaac_fr3_sim/scripts/run_fr3_scene.py
```

新增：

```text
--disable-scene-collisions
```

它只用于诊断，会停止为 `/World/Ground`、`/World/Worktable`、`/World/Banana` 临时创建 PhysX collider，但仍可冻结香蕉。不能作为最终抓取配置。

该脚本自检已通过。

## 4. 已完成的实测及结论

### 4.1 Isaac 小幅关节驱动

`--drive-smoke-test --test-frames 180` 已通过。

注意：带 `--test-frames 180` 时，约 6 秒后出现：

```text
Simulation App Shutting Down
```

是脚本达到 180 帧后主动收尾，不是崩溃。RTX scene-db 的 transform-history 警告只影响长曝光传感器运动表现，也不是退出原因。

结论：USD、Isaac 基础 PhysX position-drive 和小幅 `j6` 运动可用。

### 4.2 固定目标的完整多点预抓取

使用固定快照：

```yaml
frame_id: base_link
x: -0.1394220062
y: -0.5772783654
z: 0.0863397935
```

实测日志：

```text
[bridge] accepted /fairino3_controller/joint_trajectory: 40 point(s), 14.295s
[trajectory] forwarded full trajectory (41 -> 40)
[trajectory] reached target, max error 0.0218 rad after 2.0s settle
Inspect pregrasp: tcp_xyz=(-0.1310 -0.5791 0.1636)
target_in_tcp_xyz=(-0.0054 -0.0090 0.0771)
process has finished cleanly
```

结论：

- Windows Isaac、WSL、DDS、MoveIt 和完整多点轨迹已端到端验证；
- 末点压缩是此前大动作不稳定的重要原因，已不再使用；
- 预抓取横向误差约 `10.5 mm`；
- 当前 TCP 相对视觉原始目标约高 `77 mm`。任务节点还使用 `target_z_offset=-0.03`，所以受限下降的安全高度以校正后的 `tz_obj` 计算，不能仅按原始视觉 Z 判定。

### 4.3 单次 10 mm 下降

受限模式正常生成下降：

```text
Single descend: current_z=0.1659 target_z=0.1559 distance=0.0100 safe_z=0.1363
[bridge] accepted /fairino3_controller/joint_trajectory: 4 point(s), 2.000s
```

随后：

```text
Simulation App Shutting Down
[trajectory] aborted while waiting: joint state stale for 2.03s
single_descend_execute_failed
```

结论：

- 安全模式正确工作：没有闭爪、没有抬升、没有 fallback 或追加动作；
- Isaac 在下降轨迹运行期间先结束，C 才因 `/joint_states` 过期中止；
- 没有 `SAFETY STOP`，因此不是已实现的关节限位保护触发；
- 关闭 viewport RGB-D 后仍出现同一问题，已排除 active viewport / Replicator RGB-D 为主因；
- 夹爪代理空闲时不会发布命令，且单次下降模式不会发送夹爪 goal；它不是当前首要嫌疑。

## 5. 当前判断

已排除或已基本排除：

- WSL 与 Windows ROS 2 基础互通；
- ROS Domain 设置导致的本次问题；
- YOLO/目标快照；
- 单点末端压缩造成的预抓取失败；
- viewport RGB-D 发布链路；
- 单次下降流程中的夹爪、闭爪和抬升逻辑。

仍待定位：

```text
预抓取后 10 mm Cartesian 下降姿态下的 Isaac/PhysX 退出
```

优先嫌疑顺序：

1. Ground、Worktable 或冻结香蕉的场景碰撞体在下降时产生异常接触；
2. 机械臂自身碰撞体或特定下降姿态下的 PhysX 接触；
3. 该姿态对应的 position-drive 增益、力矩或关节驱动稳定性；
4. Isaac/Kit 6.0.1 对此 USD 的运行时问题。

尚无证据表明“安装 WSL 中的低版本 Isaac Sim”能解决此问题；现有成功的完整多点预抓取反而表明当前 Windows Isaac + WSL ROS 架构可用。

## 6. 后续验证计划

### 阶段 A：确认场景碰撞隔离结果（当前优先级最高）

用以下 A 端命令启动 Isaac：

```powershell
. .\setup_isaac_ros2_env.ps1 -IsaacRoot C:\app\issacsim -DomainId 42

& C:\app\issacsim\python.bat .\scripts\run_fr3_scene.py `
  --asset .\assets\fairino3_robotiq_complete.usd `
  --domain-id 42 --freeze-banana --disable-scene-collisions `
  --camera-width 320 --camera-height 240 --camera-tick-rate 5
```

必须确认 A 输出：

```text
[bridge] scene colliders disabled for this diagnostic run
```

随后仅运行固定目标 + `single_descend_enable:=true`，不要启动夹爪代理。

判定：

- 下降成功：排查 Ground/Worktable/Banana 的 collider、接触材质与碰撞过滤；
- 仍退出：进入阶段 B。

### 阶段 B：机器人自身碰撞与驱动隔离

1. 保存 A、C、E 三个终端从预抓取开始的完整日志；
2. 使用 `inspect_collisions.py` 统计机器人实例代理上的碰撞体；
3. 在不改变控制轨迹的前提下，单独检查机器人 self-collision；
4. 针对下降的四点轨迹，记录每一步的关节目标、实际关节状态和是否出现非有限值；
5. 仅在确认碰撞无关后，逐级降低 `j4/j5/j6` 的 position-drive 刚度/阻尼或缩小下降距离进行诊断。

### 阶段 C：几何和接触验证（仅在下降稳定后）

1. 重复至少 3 次“预抓取 + 10 mm 下降”；
2. 标定 TCP 到指尖、香蕉中心与 `target_z_offset`；
3. 再单独验证夹爪关闭；
4. 再单独验证抬升；
5. 最后解除香蕉冻结、恢复动态刚体和接触参数，才可以评估真正抓取。

## 7. 安全边界

在下降稳定前，禁止：

- 运行不带限制的 `run_pick_banana.ps1`；
- 自动闭夹爪；
- 自动抬升；
- 下降失败后的关节规划 fallback、补偿或重试；
- 宣称香蕉已抓取。

每次失败后，先保存 A/C/E 日志，再停止后续节点；不要在 Isaac 已停止且 `/joint_states` 过期时继续发送 Action。
